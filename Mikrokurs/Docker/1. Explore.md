---
aliases:
  - Docker — Explore
---
![[0. Plan.md#^docker-explore-steps]]

# 1. Maszyna Wirtualna
1. Ściągnij VirtualBox, jeśli jeszcze go nie masz.
2. Ściągnij Obraz ZSK.
3. Zaktualizuj zyppera:

```console
# zypper ref
All repositories have been refreshed.
```
# 2. Zainstaluj Docker
1. Pobierz dockera
```console
# zypper in -y docker docker-buildx docker-compose
Loading repository data...
Reading installed packages...
Resolving package dependencies...

The following 2 recommended packages were automatically selected:
  criu docker-rootless-extras

The following 17 NEW packages are going to be installed:
  catatonit containerd criu docker docker-bash-completion docker-buildx
  docker-compose docker-compose-switch docker-rootless-extras fuse-overlayfs libnet9
  libslirp0 python3-ipaddr python3-protobuf rootlesskit runc slirp4netns

17 new packages to install.

...

(17/17) Installing: docker-compose-switch-1.0.5-bp156.2.5.x86_64 ..........[done]

```

2. Uruchom usługę
```console
# service docker start
# service docker status
```

3. Sprawdź działanie samego dockera:
```console
# docker version
# docker info
```

To create the `docker` group and add your user:

5. Create the `docker` group.
```console
# groupadd docker
```

6. Add your user to the `docker` group.
```console
$ sudo usermod -aG docker $USER
```

7. Log out and log back in so that your group membership is re-evaluated.
> If you're running Linux in a virtual machine, it may be necessary to restart the virtual machine for changes to take effect.

You can also run the following command to activate the changes to groups:
```console
$ newgrp docker
```


> [!NOTE] Source
> https://docs.docker.com/engine/install/linux-postinstall/

# 3. Przykład
A Docker container is a software bucket comprising everything necessary to run the software independently. A container includes the main software component along with all of its dependencies (binaries, libraries, configuration files, scripts, and so on).

## Using ready-to-pull images

```console
$ docker search ubuntu

$ docker pull ubuntu

$ docker images
```

## Starting a container

```console
$ docker run --name cont1 -i –t ubuntu /bin/bash

$ docker ps -a
```

### Setting a container’s hostname
```console
$ docker run -h c1 ...
```

## Stopping a container

```console
$ docker stop 72c9629
```
or:
```console
$ docker stop cont1
```

## Starting a previously stopped container
```console
$ docker start cont1

$ docker attach cont1
```

## Removing a stopped container
```console
$ docker rm cont1
```

### Automatically removing a container when it exits
```console
$ docker run --rm ...
```

# 4. Odpalenie przykładowego compose'a
5. https://docs.docker.com/get-started/introduction/develop-with-containers/